'use strict';

// Define the `core.phone` module
angular.module('core.tiles', ['ngResource',]);
angular.module('core.tasks', ['ngResource',]);
angular.module('core.tile', ['ngResource',]);
angular.module('core.task', ['ngResource',]);

